// 产品铁规则。
// 灵魂见 MANIFESTO「封存即消失」—— 要足够久,久到你真的忘了。

/**
 * 封存的最短跨度(天)。
 * 送达日期必须 ≥ 今天 + MIN_SEAL_DAYS。
 *
 * 整个 app 只在这里定义这个数字 —— 想调下限,只改这一行。
 * (将来后端也会用同一个值校验:前端拦、后端也拦。)
 */
export const MIN_SEAL_DAYS = 15;
